[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["venv", "alembic/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.black]
line-length = 100
target-version = ["py312"]
extend-exclude = "venv"

[tool.pytest.ini_options]
testpaths = ["tests"]
